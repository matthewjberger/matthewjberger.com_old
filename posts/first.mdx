---
title: "First Post"
---

# First Post

Test post

```javascript
export const getStaticProps = async ({ params }) => {
  const post = await getSinglePost(params.slug)
  return {
    props: { ...post },
  }
}
```
